- name: Write and validate creds
  run: |
    cat > /tmp/gws-creds.json <<'JSON'
    ${{ secrets.GWS_CREDENTIALS_JSON }}
    JSON
    # Validate JSON and required fields
    jq '.client_id, .client_secret, .refresh_token' /tmp/gws-creds.json || exit 1
